vibe
*.exe
dist/
.env

# .vibe/compiled/ is intentionally NOT ignored — cached scripts should be
# committed so CI use them without LLM calls.

.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.venv/